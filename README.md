# READ ME

## Running Scraper
1. Navigate to the `scraper/` directory.
2. Install dependencies: `npm install`  
3. Run the scraper: `npm start`  
   - This will execute the Playwright script and output structured JSON to the console.

## Running APIs

## DB